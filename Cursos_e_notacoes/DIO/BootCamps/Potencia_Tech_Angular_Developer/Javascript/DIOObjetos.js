<<<<<<< HEAD
//Classe(definição) é o que deveria ser: 
//é uma instancia(ocorrencia) de pessoa
/*class Pessoa {
    nome;
    idade;

    constructor (nome, idade){
        this.nome = nome;
        this.idade = idade;
        this.anoDeNascimento = 2022 - idade;
    }  
}

    function compararPessoas (p1, p2){
        if (p1.idade > p2.idade) {
            console.log(`${p1.nome} é mais velho(a) que ${p2.nome}`);
        } else if (p2.idade > p1.idade) {
            console.log(`${p2.nome} é mais velho(a) que ${p1.nome}`);
        } else {
            console.log (`${p1.nome} e ${p2.nome} tem a mesma idade`);
        }
    }

    



const vitor= new Pessoa('Vitor', 29);
const renan = new Pessoa('Renan', 29);

compararPessoas(vitor,renan);*/

/*class pessoa {
    nome;
    idade;
    
    constructor (nome, idade) {
        this.nome = nome
        this.idade = idade;
    }

    descrever(){
        console.log(`Nome: ${this.nome}; idade: ${this.idade}`);
    }
}

new pessoa('Renan',30).descrever();*/

class Carros {
    marca;
    cor;
    gastoMedio;

    constructor (marca, cor, gastoMedio){
        this.marca = marca;
        this.cor = cor;
        this.gastoMedio = gastoMedio;

    }
    
    calularGastoReais () {
        console.log(``)
    }
}



=======
//Classe(definição) é o que deveria ser: 
//é uma instancia(ocorrencia) de pessoa
/*class Pessoa {
    nome;
    idade;

    constructor (nome, idade){
        this.nome = nome;
        this.idade = idade;
        this.anoDeNascimento = 2022 - idade;
    }  
}

    function compararPessoas (p1, p2){
        if (p1.idade > p2.idade) {
            console.log(`${p1.nome} é mais velho(a) que ${p2.nome}`);
        } else if (p2.idade > p1.idade) {
            console.log(`${p2.nome} é mais velho(a) que ${p1.nome}`);
        } else {
            console.log (`${p1.nome} e ${p2.nome} tem a mesma idade`);
        }
    }

    



const vitor= new Pessoa('Vitor', 29);
const renan = new Pessoa('Renan', 29);

compararPessoas(vitor,renan);*/

/*class pessoa {
    nome;
    idade;
    
    constructor (nome, idade) {
        this.nome = nome
        this.idade = idade;
    }

    descrever(){
        console.log(`Nome: ${this.nome}; idade: ${this.idade}`);
    }
}

new pessoa('Renan',30).descrever();*/

class Carros {
    marca;
    cor;
    gastoMedio;

    constructor (marca, cor, gastoMedio){
        this.marca = marca;
        this.cor = cor;
        this.gastoMedio = gastoMedio;

    }
    
    calularGastoReais () {
        console.log(``)
    }
}



>>>>>>> ba92442e26652e6cc0f2d1dbb1f9bfb272676897
