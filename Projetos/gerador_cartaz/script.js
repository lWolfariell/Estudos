<<<<<<< HEAD


function display(nome1, preco1, custo1 ){

    document.write ("<h1>" + nome1 +"</h1>");
    document.write ("<h1>R$ " + preco1 +"</h1>");
    document.write ("<h1>PREÇO DE " + custo1 +"</h1>");
}

function calculalKgL(preco,gramasLitro){

    return ((preco / gramasLitro) * 1000).toFixed(2);

}

let produto = calculalKgL (10,300)


let nome1 = 
let preco1 = 
let custo1 = 








/*const produtosTextArea = document.getElementById("produtos")

exibir.addEventListener("click", () => {
  
  function validarDados() {
    const dadoscliente = produtosTextArea.value.split("\n");
    const produtos = produtosTextArea.value.replace(/[][0-9]/g, "");
    produtosTextArea.value = produtos;
    return dadoscliente;

  }

  const dadoscliente = validarDados();
  const precos = precoTextArea.value.split("\n");
  const kgs = kgTextArea.value.split("\n");

  const listaProdutos = [];

  for (let i = 0; i < dadoscliente.length; i++) {
    const produto = {
      nome: dadoscliente[i],
      preco: precos[i],
      kg: kgs[i],
    };
    listaProdutos.push(produto);
  }

  console.log(listaProdutos);
});*/




=======


function display(nome1, preco1, custo1 ){

    document.write ("<h1>" + nome1 +"</h1>");
    document.write ("<h1>R$ " + preco1 +"</h1>");
    document.write ("<h1>PREÇO DE " + custo1 +"</h1>");
}

function calculalKgL(preco,gramasLitro){

    return ((preco / gramasLitro) * 1000).toFixed(2);

}

let produto = calculalKgL (10,300)


let nome1 = 
let preco1 = 
let custo1 = 








/*const produtosTextArea = document.getElementById("produtos")

exibir.addEventListener("click", () => {
  
  function validarDados() {
    const dadoscliente = produtosTextArea.value.split("\n");
    const produtos = produtosTextArea.value.replace(/[][0-9]/g, "");
    produtosTextArea.value = produtos;
    return dadoscliente;

  }

  const dadoscliente = validarDados();
  const precos = precoTextArea.value.split("\n");
  const kgs = kgTextArea.value.split("\n");

  const listaProdutos = [];

  for (let i = 0; i < dadoscliente.length; i++) {
    const produto = {
      nome: dadoscliente[i],
      preco: precos[i],
      kg: kgs[i],
    };
    listaProdutos.push(produto);
  }

  console.log(listaProdutos);
});*/




>>>>>>> ba92442e26652e6cc0f2d1dbb1f9bfb272676897
